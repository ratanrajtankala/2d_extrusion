// sharedState.js
const sharedState = {
    currentMode: 'draw',
    drawnPoints: [],
    selectedMesh: null,
    selectedVertex: null,
    // Add other shared variables as needed
};

export default sharedState;